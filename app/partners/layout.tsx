import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Partner Program for Lenders, Title & Home-Service Pros',
  description:
    'Sponsor a team of real estate agents: your branding on every open house visitor email, plus the leads. Built for RESPA-conscious co-marketing.',
}

export default function PartnersLayout({ children }: { children: React.ReactNode }) {
  return children
}
